---
title: "Fishing Communities Map_Sep 2025"
format: html
editor: visual
---

## Quarto

Quarto enables you to weave together content and executable code into a finished document. To learn more about Quarto see <https://quarto.org>.

## Running Code

When you click the **Render** button a document will be generated that includes both content and the output of embedded code. You can embed code like this:

```{r Libraries}
library(tidyverse)
library(here)
library(janitor)
library(sf)
library(mapview)
```

```{r Reading in the Shape Files}
hem_data<- st_read(here("/home/shares/ohi/OHI_GOC/_raw_data/Hem_data/goc_fish_comms", "goc_fish_comms.shp"))
mapview(hem_data, zcol= "NOM_LOC")
```

